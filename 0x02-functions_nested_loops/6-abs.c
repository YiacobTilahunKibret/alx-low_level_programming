#include "main.h"

/**
 * _abs - Entry point
 * Description: retuns absloute value of a number
 * @n: parameter declared
 * Return: absolute value of a number
 */

int _abs(int n)
{
	if (n < 0)
	{
		n = -n;
		return (n);
	}
	else
	{
		return (n);
	}
}
